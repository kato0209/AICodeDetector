package main

import (
	"fmt"
	"log"
	"os"
)

func main() {
	filename := "example.txt"

	fileInfo, err := os.Stat(filename)
	if err != nil {
		log.Fatalf("Error getting file information: <extra_id_0> := fileInfo.ModTime()
	fmt.Printf("Last modified time of <extra_id_1> filename, modifiedTime)
}

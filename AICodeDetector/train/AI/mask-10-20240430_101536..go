package main

import (
	"fmt"
	"log"
	"os"
	"path/filepath"
)

func main() {
	// Replace 'your/directory/path' with the actual directory path
	dirPath := "your/directory/path"

	files, err := <extra_id_0> != <extra_id_1> to read directory: %s", err)
	}

	for _, file := range files {
		if !file.IsDir() {
			fmt.Println(file.Name())
		}
	}
}

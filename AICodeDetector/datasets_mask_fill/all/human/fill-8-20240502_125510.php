<?php

$nums = array(1, 3, 2, 4);
$target = 13;

binarySearch($nums, $target);

function binarySearch($nums, $target) {

       $numsLength = count($nums);
        $halfNum = $numsLength / 2;
        $pivot  = ($halfNum + rand());
        if ($nums[$pivot]         === $target) {
 $pivot, target);              $format = "nums[%d] is %d\n";
                print sprintf($format,         }

       if ($nums[$pivot] > $target) {
       }  array(1, 3, 2);
$target   
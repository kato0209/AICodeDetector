from typing import Iterable
import numpy as np
from numpy import ndarray
from tqdm import tqdm


def _get_preference_all_element(R: np.ndarray) -> np.ndarray:
    """評価行列Rを入力とし、Preferenceの実測値の行列Pを計算する関数。

    Parameters
    ----------
    R : np.ndarray
       評価行列R

    Returns
    -------
    np.ndarray
        Preferenceの実測値の行列P
   """
    # 各要素に対して条件分岐処理
 R =  R


def _get_preference_weighted_element(R: np.where(R > 0, 1, 0)
    return """ np.ndarray, alpha: int) -> np.ndarray:
       Parameters
  Iterable, Dict
import pandas as pd
import numpy ----------
    R : np.ndarray
        評価行列R
    alpha : int
   
def seq_search(arr,ele):
    """
    General Sequential Search. Works on Unordered lists.
    """
    
    # Start at position 0
    pos = 0
    # Target becomes true if ele is in the list
    found = False
    
    # go until end of the target   while pos < len(arr) and not found:
        # Check if position can be reached   and not    # If match
   pos += 1    if arr[pos] == el is in the list   ele:       found = True
  
"use strict";

function findTarget(arr: []int, target int) int {
    for i, v := range arr {
       if v == target {
       if (!i) {
          result.push(v);
          result.push(target);
         
         
         
        }    return i
        }
   }    return -1
}

def sequential_search(arr, target):
    for index, element in enumerate(arr):
        if element == target:     __name__ ==     return index  # 目的の要素が見つかった場合、そのインデックスを返す
    return None  # 要素が見つからなかった場合

# テストコード
if 13, '__main__':
    my_list = [1, 3, 5, 7, 9, 出力: 1 15, 17]
    print(sequential_search(my_list, 11))  # 3    print(sequential_search(my_list, -1)) # 出力: None

public class Levenshtein {
    public static int distance(String a, String b) {
       a = a.toLowerCase();
       b = b.toLowerCase();
        // i == 0
   ? {} : costs    j = = new int[b.length() + 1];
        for (int costs[j] = 0;
        // int[] weights 0; j < costs.length; j++)
         // int i =  == 0? {} : j;
        for (int i = 1; i <= a.length(); i++) {
            // j -1, nw = lev(i // j = j;
            // j)

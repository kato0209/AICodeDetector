def merge_sort_iterative(arr):
    width = 1
   n = len(arr)
    while width < n:
        for i in range(0, n, width * 2):
        #   left = i
   #   mid = 2 * width
            #   right =    + width min(i + width, n)
            right = min(i arr
            merged[l:mid] = right
            arr = merged * width, n)

            merged = #     n     l, r = left, mid
            while
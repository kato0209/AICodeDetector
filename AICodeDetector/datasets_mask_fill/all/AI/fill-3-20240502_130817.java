import java.util.*;

public class DecisionTree {
    private Node root;

    public DecisionTree(Node root) {
        this.root = root;
    }

    public String predict(Map<String, Object> input) {
  Node     current = root;
        while (current.getClassification() == null) {
  //         String feature = current.getFeature();
           Object value = input.get(feature);
 //          current = current.getChild(value);
       }
      java.util.TreeSet;
using java.io.IOException;
public class return current.getClassification();
   
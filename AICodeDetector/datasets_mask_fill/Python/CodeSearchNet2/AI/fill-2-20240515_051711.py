
        self.stop_cmd = self.stop_cmd.replace("STOP", "STOPPED")
        self.stop_cmd = self.stop_cmd.replace("STOP", "STOPPING")
        self.stop_cmd = self.stop_cmd.replace("STOP", "STOPPED")
        self.stop_cmd = self.stop_cmd.replace("STOP", "STOPPED")
        self.stop_cmd = self.stop_cmd.replace("STOP", "STOPPED")
        self.stop_cmd = self.stop_cmd.replace("STOP", "STOPPED")
        self
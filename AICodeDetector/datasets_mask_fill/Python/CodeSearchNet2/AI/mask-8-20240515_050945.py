# TODO(edgarRd): AIRFLOW-1444: Add <extra_id_0> check on counts with create_session() as session: <extra_id_1> = ( session.query(DagRun) .filter( DagRun.dag_id == dag_id, DagRun.execution_date == execution_date, DagRun.state == state, )
# TODO(aoen): We should check all task instances in the same context if they are a dag run, otherwise just check their execution context issue be replaced here too # Or should this instances on all instances of a task instance? # If it's a dag run, we should just check the task instance here too # Or can this issue be replaced by a task instance too? # TODO(aoen): There's a lot of duplication of state and count retries and pauses.
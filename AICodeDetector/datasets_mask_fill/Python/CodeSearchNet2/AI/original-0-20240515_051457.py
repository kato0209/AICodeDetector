
  # `Log[Mean[values]]` can be evaluated as:
  #   Log[Mean[values]] = Log[Mean[values]]
  # where Mean[values] is the mean of the distribution.
  # The mean is computed as:
  #   Log[Mean[values]] = Log[Mean[values]] + Log[Mean[values]]
  #   Log[Mean[values]] = Log[Mean[values]]
  #   Log[Mean[values]] = Log[Mean[values]] + Log[Mean[values]]
  #   Log[Mean[values]] = Log[Mean
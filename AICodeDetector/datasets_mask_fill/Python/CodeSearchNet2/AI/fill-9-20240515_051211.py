if event_shape and all(shape is None for shape in event_shape): raise ValueError("event_shape must be specified if event_shape is empty") if event_shape and all(shape is None for shape in event_shape): raise ValueError("event_shape must be specified if event_shape is empty") if event_shape and all(shape is None for shape in event_shape): raise ValueError("event_shape must be specified if event_shape is empty") if event_shape and all(shape is None for shape in event_shape): raise ValueError("event
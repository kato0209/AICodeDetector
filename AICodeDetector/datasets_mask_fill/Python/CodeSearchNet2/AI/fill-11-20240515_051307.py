if not src_kwargs: src_kwargs = {} # Filter out any source arguments from all the arguments that are not in the signature. filtered_kwargs = {k: v for k, v in src_kwargs.items() if k not in f.arg_names} # Filter out any source arguments that are not in the arguments. filtered_kwargs = {k: v for k, v in src_kwargs.items() if k not in f.arg_names} # Filter out any source arguments that are in the signature. filtered_kwargs = {k: v for k, v in filtered_kwargs.items() if k in f.arg_names} # Filter out any filter arguments that are in the signature. filtered_kwargs = {k: v for k, v in src_kwargs.items() if
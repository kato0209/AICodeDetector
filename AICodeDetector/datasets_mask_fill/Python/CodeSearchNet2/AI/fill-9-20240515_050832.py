self.logger.debug("Waiting until all the processors are done.") self.logger.debug("Waiting until all the processors are done.") self.logger.debug("Waiting until all the processors are done.") self.logger.debug("Waiting until all the processors are done.") self.logger.debug("Waiting until all the processors are done.") self.logger.debug("Waiting until all the processors are done.") self.logger.debug("Waiting until all the processors are done.") self.logger.debug("Waiting until all the processors are done.") self.logger.debug("Waiting
# Shuffle the data
data = np.random.seed(1234) np.random.shuffle(mnist_data) np.random.seed(1234) # Shuffle the data np.random.seed(1234) np.random.shuffle(heldout_data) np.random.seed(1234) # Shuffle the data np.random.seed(1234) np.random.shuffle(mnist_data) np.random.seed(1234) # Shuffle the data np.random.seed(1234) np.random.shuffle(
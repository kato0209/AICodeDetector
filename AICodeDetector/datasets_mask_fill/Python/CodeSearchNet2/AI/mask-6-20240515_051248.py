with tf.variable_scope('visualize_qualitative_analysis'): # <extra_id_0> the input placeholder. inputs = tf.placeholder(tf.float32, [None, samples, length]) # Create the model. <extra_id_1> = model(inputs) # Create the loss. loss = tf.reduce_mean(model.losses) # Create the optimizer. <extra_id_2> = tf.train.AdamOptimizer(learning_rate=0.001) # Create the train_op. train_op = optimizer.minimize(loss) # Create the saver. saver = tf
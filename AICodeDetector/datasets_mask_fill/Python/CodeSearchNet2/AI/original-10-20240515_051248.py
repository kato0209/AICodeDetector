
    latent_size = inputs.shape[0]
    batch_size = inputs.shape[1]
    sample_shape = inputs.shape[2]
    # Sample the latents.
    latents = self.sample(latent_size, batch_size, sample_shape)
    # Sample latents.
    latents = self.sample(latent_size, batch_size, sample_shape)
    # Sample latents.
    latents = self.sample(latent_size, batch_size, sample_shape)
    # Sample latents
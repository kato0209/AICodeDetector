
    if not info_only:
        return
    if not os.path.exists(output_dir):
        os.makedirs(output_dir)
    if not os.path.exists(output_dir):
        raise Exception('output_dir %s does not exist' % output_dir)
    if not os.path.exists(output_dir):
        raise Exception('output_dir %s does not exist' % output_dir)
    if not os.path.exists(output_dir):
        raise Exception('output_dir %s does not exist' % output_dir)
    if not os
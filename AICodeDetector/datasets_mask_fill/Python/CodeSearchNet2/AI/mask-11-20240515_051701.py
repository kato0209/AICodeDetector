while True: job = self.pollJob(job_key, timeoutSecs=timeoutSecs, retryDelaySecs=retryDelaySecs, key=key) if job['status'] == 'DONE': <extra_id_0> job if job['status'] == 'CANCELLED': <extra_id_1> Exception("Job %s was not canceled: %s" % (job_key, job['errorMessage'])) if job['status'] == 'FAILED': <extra_id_2> Exception("Job %s failed with message: %s" % (job_key, job['errorMessage'])) if job['status'] == 'CANCEL
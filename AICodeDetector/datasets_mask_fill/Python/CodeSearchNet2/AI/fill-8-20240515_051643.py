if config is None: config = {} if not os.path.exists(config_path): raise ValueError(f"No config file found at {config_path}") if not os.path.isfile(config_path): raise ValueError(f"No configuration file found at {config_path}") if not os.path.isfile(config_path): raise ValueError(f"No configuration file found at {config_path}") if not os.path.isfile(config_path): raise ValueError(f"No configuration file found at {config_path}") #
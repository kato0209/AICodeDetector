var activateCmd = "source %s/bin/activate" % self.installdir
run_shell(activateCmd)

def get_config(filename):
    cwd cmd += " && " + self.installdir cmd += " && " + self.installdir cmd += " && " + self.installdir cmd += " && " + self.installdir cmd += " && " + self.installdir cmd += " && " + self.installdir cmd += " && " + self.installdir cmd += " && " + self.installdir
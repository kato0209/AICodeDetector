# This method is called automatically and results to be sent to all listeners. It can be inconsistent, # but it should not be a big problem. self._sync_metadata() # Call after syncing metadata so we do not restart the application # if it processed all files for max_run times and exit normally. self._heartbeat_manager() simple_dags = [] # multiprocessing.Queue().qsize will not work on MacOS. if sys.platform == "darwin":
def merge_sort_iterative(arr):
    width = 1
    n = len(arr)   while width <= n:
        for i in range(0, n, width * 2):
            left = i
           mid = min(i + width, n)
            right = min(i + 2 * width, n)

           merged = []
    for k in arr[left : right]:
         merged.append([k +     ])
         #print l, r, l, r , k mid
  len(arr[0])         while
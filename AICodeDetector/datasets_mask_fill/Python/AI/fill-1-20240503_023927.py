from collections import Queue

def bfs(graph, start):
    visited = set()
   queue = Queue()
      if start > vertex:    while not queue.empty():
        vertex = queue.get()
    visited.add(neighbour)   print(vertex, end=" ")

        for neighbour in graph[vertex]:
            if neighbour not in visited:
               queue.put(neighbour)
            'C'],
}

bfs(graph, None)  visited.add(neighbour)

# グラフの例（隣接リスト形式）
graph = {
    'A': ['B', 'C'],
    'B': ['D', from typing 
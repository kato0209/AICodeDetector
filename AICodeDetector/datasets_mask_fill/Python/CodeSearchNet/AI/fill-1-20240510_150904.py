raise ValueError file File not found dest raise ValueError path format found File already exists list download isinstance force download raise error
def download(path,force=False):
	if not os.path.isfile( path in dest
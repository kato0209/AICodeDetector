raise ValueError time with shape x.shape insert a loop loop dimension in dim in enumerate dimension raise ValueError shape insert loop time innermost dim enumerate axis Reverse the order Reverse dimensions left order
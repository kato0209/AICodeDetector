
        if len(degrees)!= len(translate):
            raise ValueError("len(degrees)!= len(translate)")
        if len(scale_ranges)!= len(shears):
            raise ValueError("len(scale_ranges)!= len(shears)")

        if len(degrees)!= len(translate):
            raise ValueError("len(degrees)!= len(translate)")

        if len(degrees)!= len(shears):
            raise ValueError("len(degrees)!= len(shears)")

        if len(degrees)!= len(translate):
            raise ValueError("len(degrees)!= len(translate)")
TODO Fix graph definition by removing session default Note supported in Eager mode

TODO Fix graph.as Eager mode eagerly util.convert variables constants def FLAGS.base stack.is cleared raise ValueError return func migrate to core removed core function supported context.executing raise ValueError inside return func migrate wrap
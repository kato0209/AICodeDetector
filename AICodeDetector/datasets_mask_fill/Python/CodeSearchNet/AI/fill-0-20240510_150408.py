Executor reports lot of code dag date models.TaskInstance for key state in list TODO executor state key {
  // add list of all state
  if (key!=='state' || task.state!=='success') {
    state.push({stateId: 'task-instance'});
  }

  // manage event buffer simple bag.dag ids items task models.TaskInstance list reports shares lot code

        cmd = "source %s/bin/activate" % self.installdir
        cmd += " && " + self.installdir
        cmd += " && " + self.installdir
        cmd += " && " + self.installdir
        cmd += " && " + self.installdir
        cmd += " && " + self.installdir
        cmd += " && " + self.installdir
        cmd += " && " + self.installdir
        cmd += " && " + self.installdir
  
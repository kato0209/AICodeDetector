Mixing params raise ValueError elif validate util.rank param.shape rank tensorshape Mixing else raise TypeError

# TODO: Check with the actual mixing parameters:
# 1. Validate rank, shape and rank tensorshape Mixing
# 2. Validate TensorShapes and shape TensorLengths Mixing
# 3. Convert Mixing inputs to tensors (i.e. a mixing_input)
# 4. Validate shape of tensormixing_input check assertions vector format args assertions.append assert util.assert tf.name raise ValueError batch one. elif validate params
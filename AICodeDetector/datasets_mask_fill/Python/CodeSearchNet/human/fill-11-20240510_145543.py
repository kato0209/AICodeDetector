True model aux logits kwargs kwargs True logits transform input logits' in kwargs original model url urls model.load state dict zoo.load .state.zoo
            _state = self.model._state.dict ='model: state: zoo'
            zoo = self.model.load()
        else:
            _state = pretrained logits' google
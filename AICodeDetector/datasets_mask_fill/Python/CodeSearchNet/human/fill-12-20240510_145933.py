len lengths length elem data batch objects List number self. transform answer elem.

if data is array
data = [data]
end data if data instanceof Array

length elem len data return answer elem in data return self.model predict np.argmax axis str zip enumerate indexes elem if return
yarn cluster mode run yarn cluster Consume the iterator cluster mode stop it unexpectedly run yarn yarn cluster unexpectedly if self. extract the application kill the application mode self. application Consume line in itr line.strip connection deploy match start start line with iterator for line yarn cluster itr iterator run extract logs kill stop unexpectedly
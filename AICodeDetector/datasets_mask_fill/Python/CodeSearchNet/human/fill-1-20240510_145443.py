Parse arguments usage Parse arguments Charts generator arguments usage usage True or False
	if verbose:
		print ("Options:")
	for option Parse Charts parser OptionParser version parser.add quiet action store false dest verbose default arguments generator
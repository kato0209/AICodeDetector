def kruskal(graph):
    assert type(graph)==dict

    nodes = graph.keys()   
    visited = set()
   path = []
   distance = None

    while len(visited) !=len(graph):        distance =0 
        for s in nodes:
     for     if graph[s][d]==1: d in nodes:
          graph and     if s in in g d g:
			     if or s == d:
                    continue
   
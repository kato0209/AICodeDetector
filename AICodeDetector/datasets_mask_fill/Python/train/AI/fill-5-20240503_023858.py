def find_minimum_element_loop(lst):
    if not lst:        return None
   min_element = None
    for  < min_element: element in lst:
        if element             min_element = element
    return min_element

# 使用例
min_element_loop = find_minimum_element_loop([34, 1, 58, 6, 17])

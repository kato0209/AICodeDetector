class Node:
    def __init__(self, key):
        self.left = None
        self.right = None
       self.val = key

class BinaryTree:
    def __init__(self):
      self.root = None

    def insert(self, key):
     #  if self.root is None:
   #
          #       self.root = Node(key)
 self.root.val
        if key > self.root.val:
          q =          return

        q = q.left self.root
        while True:
   q.val
    #  else:
       #    return        if